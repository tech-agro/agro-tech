"""Schema da entidade parametro_monitoramento."""

from dataclasses import dataclass

@dataclass(slots=True)
class ParametroMonitoramentoSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
