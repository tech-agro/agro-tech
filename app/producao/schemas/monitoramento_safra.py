"""Schema da entidade monitoramento_safra."""

from dataclasses import dataclass

@dataclass(slots=True)
class MonitoramentoSafraSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
