"""Schema da entidade ordem_producao."""

from dataclasses import dataclass

@dataclass(slots=True)
class OrdemProducaoSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
