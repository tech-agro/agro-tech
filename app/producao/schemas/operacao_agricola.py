"""Schema da entidade operacao_agricola."""

from dataclasses import dataclass

@dataclass(slots=True)
class OperacaoAgricolaSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
