"""Schema da entidade atividade_agricola."""

from dataclasses import dataclass

@dataclass(slots=True)
class AtividadeAgricolaSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
