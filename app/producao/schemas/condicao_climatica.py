"""Schema da entidade condicao_climatica."""

from dataclasses import dataclass

@dataclass(slots=True)
class CondicaoClimaticaSchema:
    """Placeholder de setup para contratos de entrada/saida."""

    pass
